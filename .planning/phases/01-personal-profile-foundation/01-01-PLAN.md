---
phase: 01-personal-profile-foundation
plan: 01
plan_number: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - INSTAGRAM_SETUP.md
  - CLAUDE.md
  - .env.example
  - supabase/migrations/20260531113000_configure_auth_profile_and_avatars.sql
  - supabase/rollbacks/
autonomous: true
requirements:
  - SETUP-01
  - SETUP-02
  - BRAND-01
  - BRAND-02
  - QUAL-01
  - QUAL-02
tags:
  - docs
  - setup
  - env
  - supabase
  - vercel
  - phase-1

must_haves:
  truths:
    - "README explains local setup from .env.example to .env.local, including required Supabase URL and anon key."
    - "README documents current production URL and Vercel/Supabase redirect expectations without exposing secrets."
    - "README or setup docs explain that Instagram/Metricool credentials are optional until integrations are enabled."
    - "Supabase setup docs mention migrations, avatars bucket, auth trigger, and redirect URLs."
    - ".env.example contains empty placeholders for secrets and public branding defaults only."
    - "No docs instruct the user to commit .env.local or Supabase .temp metadata."
    - "npm run lint and npm run build pass after documentation/setup edits."
  artifacts:
    - path: "README.md"
      provides: "Current setup instructions for local and Vercel/Supabase production use"
      contains: "NEXT_PUBLIC_SUPABASE_URL"
    - path: ".env.example"
      provides: "Safe copyable env template with required and optional variables"
      contains: "NEXT_PUBLIC_BRAND_NAME"
    - path: "supabase/migrations/20260531113000_configure_auth_profile_and_avatars.sql"
      provides: "Idempotent auth profile trigger and avatars bucket/policies setup"
      contains: "on_auth_user_created"
    - path: "supabase/rollbacks/"
      provides: "Rollback SQL files kept outside migrations so db push does not apply down migrations"
  key_links:
    - from: "README.md"
      to: ".env.example"
      via: "Setup steps reference copyable env names"
      pattern: ".env.example"
    - from: "README.md"
      to: "supabase/migrations/20260531113000_configure_auth_profile_and_avatars.sql"
      via: "Supabase setup section describes avatars/auth trigger requirements"
      pattern: "avatars"
user_setup: []
---

<objective>
Close the Phase 1 setup documentation gap by turning the current working Supabase/Vercel configuration into durable project instructions. The project already has a linked Supabase project, synced migrations, avatars bucket policies, Auth URL configuration, Vercel production env vars, and a ready production deploy. This plan records that state in the repo without exposing secrets.

Output: a developer can clone the repo, copy `.env.example`, configure Supabase/Vercel/Instagram values, understand what remains optional, and run `npm run lint` plus `npm run build` successfully.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-personal-profile-foundation/01-CONTEXT.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/CONCERNS.md
@README.md
@.env.example

<current_state>
- Supabase project ref: `cbrnaxmyektmrzgfwomq`.
- Vercel production alias: `https://socialmediadashboard-kappa.vercel.app`.
- Supabase Auth Site URL points to the Vercel alias.
- Supabase Auth Redirect URLs include localhost and Vercel aliases.
- Vercel Production env vars have been aligned with the new Supabase project.
- `.env.local` is intentionally gitignored and must not be documented with real values.
</current_state>

<threat_model>
Assets:
- Supabase anon key and URL are public configuration; service-role keys and Meta secrets are private.
- Instagram OAuth client secret and Metricool API key are private integration credentials.

Threats:
- Medium: docs accidentally expose old Meta app IDs/secrets or invite committing `.env.local`.
- Medium: Vercel/Supabase redirect mismatch blocks auth or sends users to the wrong origin.
- Low: down migrations placed under `supabase/migrations` can be applied accidentally by `supabase db push`.

Mitigations:
- Keep `.env.example` placeholders empty for secrets.
- Document public vs private env vars explicitly.
- Keep rollback files under `supabase/rollbacks`.
- Document exact redirect URL expectations for local and production.
</threat_model>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README setup sections</name>
  <files>
    README.md
  </files>
  <read_first>
    .planning/PROJECT.md,
    .planning/REQUIREMENTS.md,
    .planning/phases/01-personal-profile-foundation/01-CONTEXT.md,
    .env.example
  </read_first>
  <behavior>
    - Add a clear "Ambientes" or equivalent setup section for local and Vercel production.
    - State that Supabase URL and anon key are required.
    - State that Instagram and Metricool credentials are optional until their integrations are activated.
    - Include production URL and explain that `NEXT_PUBLIC_SITE_URL` must match the active deploy alias.
    - Mention Supabase Auth confirm-email behavior so first login expectations are clear.
  </behavior>
  <acceptance_criteria>
    - README mentions `https://socialmediadashboard-kappa.vercel.app`.
    - README mentions `NEXT_PUBLIC_SITE_URL`.
    - README explains that `.env.local` is local-only and not committed.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Document Supabase setup and rollback placement</name>
  <files>
    README.md,
    CLAUDE.md,
    INSTAGRAM_SETUP.md
  </files>
  <behavior>
    - Document `npx supabase link --project-ref ...` and `npx supabase db push` at a high level, without embedding secrets.
    - Document that rollback files live in `supabase/rollbacks/` intentionally.
    - Document avatars bucket expectations and storage policies.
    - Document Supabase Auth URL configuration for localhost and Vercel.
  </behavior>
  <acceptance_criteria>
    - A future agent can understand why `.down.sql` files are not in `supabase/migrations`.
    - A developer can validate Supabase setup by checking tables, `on_auth_user_created`, and bucket `avatars`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify env template remains safe</name>
  <files>
    .env.example
  </files>
  <behavior>
    - Keep all secret-like variables empty.
    - Keep public branding defaults as safe placeholders.
    - Do not add the real Supabase anon key, Meta app values, or Metricool key.
  </behavior>
  <acceptance_criteria>
    - `rg -n "sb_secret|INSTAGRAM_APP_SECRET=.+|METRICOOL_API_KEY=.+" .env.example` returns no exposed secret values.
    - `NEXT_PUBLIC_SUPABASE_URL=` and `NEXT_PUBLIC_SUPABASE_ANON_KEY=` stay empty in `.env.example`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run:

```bash
npm run lint
npm run build
rg -n "sb_secret|INSTAGRAM_APP_SECRET=.+|METRICOOL_API_KEY=.+" .env.example || true
```

Expected:
- lint exits 0.
- build exits 0.
- secret scan returns no real secret values.
</verification>

<success_criteria>
- [ ] Setup docs reflect actual Supabase and Vercel state.
- [ ] `.env.example` remains safe and copyable.
- [ ] Supabase migrations/rollbacks structure is explained.
- [ ] Local lint and build pass.
</success_criteria>
