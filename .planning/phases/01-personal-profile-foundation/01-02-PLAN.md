---
phase: 01-personal-profile-foundation
plan: 02
plan_number: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/page.tsx
  - app/(dashboard)/instagram/page.tsx
  - app/(dashboard)/analytics/page.tsx
  - app/(dashboard)/notifications/page.tsx
  - app/api/notifications/_shared.ts
  - app/(dashboard)/competitors/page.tsx
  - tests/e2e/smoke.spec.ts
  - tests/e2e/auth.spec.ts
autonomous: true
requirements:
  - BRAND-02
  - BRAND-03
  - SETUP-03
  - QUAL-01
  - QUAL-02
  - QUAL-03
tags:
  - ui
  - branding
  - mock-data
  - empty-states
  - tests
  - phase-1

must_haves:
  truths:
    - "No visible inherited Sabrina branding remains in user-facing app screens."
    - "Overview and Instagram Manager do not present fake follower metrics as real account data before integration is connected."
    - "Mock analytics states are explicitly labeled as demo/mock when active."
    - "Seed/demo notifications do not claim real user milestones such as 48k followers."
    - "Competitor tracker communicates manual/demo observation status where real API data is not available."
    - "Unauthenticated E2E smoke tests still pass."
  artifacts:
    - path: "app/(dashboard)/page.tsx"
      provides: "Overview cards with honest pending-integration states"
      contains: "Instagram pendente"
    - path: "app/(dashboard)/instagram/page.tsx"
      provides: "Instagram Manager cards without fake follower totals"
      contains: "Conectar"
    - path: "app/api/notifications/_shared.ts"
      provides: "Seed notifications that are safe demo/sample items, not fake real achievements"
    - path: "tests/e2e/smoke.spec.ts"
      provides: "Smoke coverage for login/protected route behavior after copy changes"
  key_links:
    - from: "app/api/analytics/route.ts"
      to: "app/(dashboard)/analytics/page.tsx"
      via: "Analytics source field controls live/metricool/mock banner"
      pattern: "source"
    - from: "app/api/notifications/_shared.ts"
      to: "app/(dashboard)/notifications/page.tsx"
      via: "Seed notification copy appears in notification center"
      pattern: "DEMO_NOTIFICATIONS"
user_setup: []
---

<objective>
Finish the visible product cleanup for Phase 1 by ensuring the app is honest about integration state. Before Instagram/Metricool are connected, the UI should invite connection or label demo data instead of implying real profile metrics.

Output: user-facing screens no longer show inherited/demo follower milestones as real data; tests remain green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-personal-profile-foundation/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md

<known_findings>
Recent audit found remaining mock/demo references in analytics, news, notifications, competitors, and docs. Analytics/news mock fallbacks are acceptable when labeled. Riskier user-facing examples are notifications and competitor/overview copy that can imply live account data.
</known_findings>

<threat_model>
Assets:
- User trust in dashboard metrics and notification center.
- User's Instagram account decisions based on displayed KPIs.

Threats:
- Medium: fake follower milestones or competitor growth messages may be mistaken for real data.
- Low: over-removing mocks can break development fallback and smoke tests.

Mitigations:
- Keep mock fallbacks, but label them as demo/sample.
- Replace fake personal achievement messages with integration-pending or sample-language copy.
- Preserve deterministic mocks where they are explicitly presented as mock.
</threat_model>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit visible hardcoded profile/KPI copy</name>
  <files>
    app/(dashboard)/page.tsx,
    app/(dashboard)/instagram/page.tsx,
    app/(dashboard)/analytics/page.tsx,
    app/(dashboard)/competitors/page.tsx,
    app/(dashboard)/notifications/page.tsx,
    app/api/notifications/_shared.ts
  </files>
  <behavior>
    - Search for old branding, fake follower totals, and fake achievement copy.
    - Keep "Conectar" and "Instagram pendente" style states where metrics are unavailable.
    - In Analytics, preserve source banners and ensure mock text remains explicit.
  </behavior>
  <acceptance_criteria>
    - `rg -n "Sabrina|48\\.2K|48\\.000|ultrapassou 48" app components lib tests README.md docs` returns no user-facing inherited/fake-profile claims except historical docs if explicitly marked as old state.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sanitize notification seed/demo copy</name>
  <files>
    app/api/notifications/_shared.ts,
    app/(dashboard)/notifications/page.tsx
  </files>
  <behavior>
    - Replace sample notifications that claim the user gained or surpassed specific follower numbers.
    - Keep notification categories/types intact so API behavior does not change.
    - Prefer "Exemplo" or integration-pending wording for seed messages.
  </behavior>
  <acceptance_criteria>
    - Seed notifications no longer imply real follower milestones for the user's account.
    - Notification UI still renders categories and state actions.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update tests for neutral default branding</name>
  <files>
    tests/e2e/smoke.spec.ts,
    tests/e2e/auth.spec.ts,
    tests/e2e/api-sources.spec.ts
  </files>
  <behavior>
    - Ensure tests assert `Instagram Dashboard` or stable route behavior, not old inherited branding.
    - Keep unauthenticated route protection checks intact.
  </behavior>
  <acceptance_criteria>
    - `npm run e2e` has unauthenticated tests passing; authenticated test may remain skipped without credentials.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run:

```bash
npm run lint
npm run build
npm run e2e
rg -n "Sabrina|48\\.2K|48\\.000|ultrapassou 48" app components lib tests README.md docs || true
```

Expected:
- lint exits 0.
- build exits 0.
- unauthenticated E2E passes.
- search does not return misleading user-facing copy.
</verification>

<success_criteria>
- [ ] No inherited branding remains in active UI/tests.
- [ ] Fake KPIs are replaced by pending/connection states.
- [ ] Mock/demo data is explicit where retained.
- [ ] E2E smoke coverage stays green.
</success_criteria>
