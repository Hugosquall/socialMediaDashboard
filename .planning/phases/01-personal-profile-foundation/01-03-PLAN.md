---
phase: 01-personal-profile-foundation
plan: 03
plan_number: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - tests/e2e/authenticated.spec.ts
  - playwright.config.ts
  - README.md
  - docs/NOTIFICATIONS_RUNBOOK.md
  - .planning/STATE.md
autonomous: false
requirements:
  - SETUP-03
  - QUAL-01
  - QUAL-02
  - QUAL-03
tags:
  - validation
  - e2e
  - production
  - auth
  - vercel
  - phase-1

must_haves:
  truths:
    - "Local unauthenticated smoke tests pass."
    - "Production Vercel deploy opens `/login` with the current app name."
    - "A real user can sign up, confirm email, and log in, or any blocker is captured explicitly."
    - "Authenticated E2E remains optional and skipped unless `E2E_AUTH_EMAIL` and `E2E_AUTH_PASSWORD` are provided."
    - "Final Phase 1 verification records exact commands run and outcomes."
  artifacts:
    - path: "tests/e2e/authenticated.spec.ts"
      provides: "Optional authenticated flow that can validate calendar access when test credentials exist"
    - path: "playwright.config.ts"
      provides: "Stable local E2E server setup"
    - path: "README.md"
      provides: "Instructions for optional authenticated E2E credentials"
  key_links:
    - from: "tests/e2e/authenticated.spec.ts"
      to: "Supabase Auth user"
      via: "E2E_AUTH_EMAIL/E2E_AUTH_PASSWORD credentials"
      pattern: "E2E_AUTH_EMAIL"
user_setup:
  - service: supabase-auth
    why: "Confirm-email is enabled; a real test account needs email confirmation before first login."
    dashboard_config:
      - task: "Create account through production app or local app"
        location: "https://socialmediadashboard-kappa.vercel.app/login"
      - task: "Confirm email before login"
        location: "User inbox or Supabase Authentication > Users if manual confirmation is needed"
    env_vars:
      - name: E2E_AUTH_EMAIL
        source: "Test account email, only for optional authenticated E2E"
      - name: E2E_AUTH_PASSWORD
        source: "Test account password, only for optional authenticated E2E"
---

<objective>
Complete Phase 1 with a clean verification pass. This plan runs the local quality gates and validates that production is usable enough for real account creation/login. Because Supabase email confirmation is enabled, the real authenticated login check may require a user action.

Output: a Phase 1 execution summary with local lint/build/E2E results, production deploy URL, and either a successful real signup/login check or a documented auth blocker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-personal-profile-foundation/01-CONTEXT.md
@tests/e2e/authenticated.spec.ts
@playwright.config.ts

<current_production>
- Vercel production alias: `https://socialmediadashboard-kappa.vercel.app`
- Supabase Auth Site URL: same Vercel alias.
- Redirect URLs include localhost and Vercel.
- Email provider, signups, and confirm email are enabled.
</current_production>

<threat_model>
Assets:
- Supabase authenticated session cookies.
- Test user credentials if provided for E2E.

Threats:
- Medium: storing real test credentials in repo or logs.
- Medium: production signup fails because email confirmation or redirect configuration is wrong.
- Low: authenticated E2E becomes flaky if it depends on external email delivery.

Mitigations:
- Keep authenticated credentials only in environment variables.
- Preserve skip behavior when test credentials are absent.
- Treat manual email confirmation as a checkpoint, not a hidden test failure.
</threat_model>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run local quality gates</name>
  <files>
    package.json,
    tests/e2e/
  </files>
  <behavior>
    - Run `npm run lint`.
    - Run `npm run build`.
    - Run `npm run e2e`.
    - Record whether authenticated E2E was skipped due to missing credentials.
  </behavior>
  <acceptance_criteria>
    - lint exits 0.
    - build exits 0.
    - E2E exits 0 with unauthenticated coverage passing.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Validate real signup/login on Vercel</name>
  <what-needed>
    1. Open `https://socialmediadashboard-kappa.vercel.app/login`.
    2. Create a test account with an email you can access.
    3. Confirm the email.
    4. Log in and verify the dashboard redirects to an authenticated page such as `/instagram` or `/calendar`.
  </what-needed>
  <how-to-verify>
    - Login succeeds without Supabase redirect errors.
    - `profiles` row is created automatically by `on_auth_user_created`.
    - Dashboard shell renders after login.
  </how-to-verify>
  <failure_handling>
    - If confirmation email does not arrive, inspect Supabase Auth > Users and email provider settings.
    - If login fails after confirmation, inspect Auth URL Configuration and browser console/network errors.
    - If profile is missing, inspect trigger `on_auth_user_created`.
  </failure_handling>
</task>

<task type="auto">
  <name>Task 3: Capture Phase 1 verification evidence</name>
  <files>
    .planning/phases/01-personal-profile-foundation/01-03-SUMMARY.md
  </files>
  <behavior>
    - Record commands run, deploy URL, Supabase state, and any manual checkpoint result.
    - If manual signup/login is not completed, mark it as a remaining checkpoint rather than claiming Phase 1 complete.
  </behavior>
  <acceptance_criteria>
    - Summary is specific enough for `/gsd-verify-work` to evaluate Phase 1.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run:

```bash
npm run lint
npm run build
npm run e2e
npx vercel inspect https://socialmediadashboard-kappa.vercel.app
```

Manual:
- Create and confirm a test user in production, then log in.
</verification>

<success_criteria>
- [ ] Local lint/build/E2E pass.
- [ ] Production `/login` is reachable.
- [ ] Real signup/login is validated or recorded as a blocking checkpoint.
- [ ] Phase 1 has enough evidence for `/gsd-verify-work`.
</success_criteria>
