---
phase: 02-instagram-connection-hardening
plan: 01
plan_number: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/analytics/sources/route.ts
  - app/(dashboard)/settings/page.tsx
  - playwright.config.ts
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - IG-02
  - IG-03
  - QUAL-01
  - QUAL-02
tags:
  - instagram
  - settings
  - token-status
  - production
---

<objective>
Expose the connected Instagram account status through the analytics sources API and render actionable token state in Settings.
</objective>

<tasks>

<task type="auto">
  <name>Return Instagram token metadata from `/api/analytics/sources`</name>
  <behavior>
    - Keep the existing `instagram` boolean for backward compatibility.
    - Add `instagramStatus.connected`, `username`, `expiresAt`, `expiresInDays` and `tokenState`.
    - Classify tokens as `active`, `expiring`, `expired`, `unknown` or `disconnected`.
  </behavior>
</task>

<task type="auto">
  <name>Render token state in Settings integrations</name>
  <behavior>
    - Load Instagram and Metricool source state from one API request.
    - Show connected username when available.
    - Show "Atenção" when the token is expired or close to expiry.
    - Keep reconnect/disconnect controls available.
  </behavior>
</task>

<task type="auto">
  <name>Validate locally</name>
  <behavior>
    - Run `npm run lint`.
    - Run `npm run build`.
  </behavior>
</task>

</tasks>

<success_criteria>
- [x] Settings can display connected Instagram username and expiry state.
- [x] API response remains backward-compatible for existing consumers.
- [x] `npm run lint` passes.
- [x] `npm run build` passes.
- [x] `npm run e2e` passes with Playwright isolated from unrelated port 3000 apps.
</success_criteria>
