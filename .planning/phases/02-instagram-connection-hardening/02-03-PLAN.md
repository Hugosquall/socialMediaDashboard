---
phase: 02-instagram-connection-hardening
plan: 03
plan_number: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/instagram-token-status.ts
  - app/api/analytics/sources/route.ts
  - app/api/notifications/_shared.ts
autonomous: true
requirements:
  - QUAL-04
  - IG-02
  - IG-04
tags:
  - instagram
  - testability
  - helpers
---

<objective>
Move Instagram token expiry classification into a pure helper so API and notification behavior can be reused and tested without calling Meta.
</objective>

<tasks>

<task type="auto">
  <name>Extract token status logic</name>
  <behavior>
    - Create a pure `getInstagramTokenStatus` helper.
    - Keep an injectable `nowMs` argument for deterministic tests.
    - Reuse the helper from analytics source status and notifications.
  </behavior>
</task>

</tasks>

<success_criteria>
- [x] Token classification no longer lives only inside an API route.
- [x] Both Settings source status and proactive notifications use the same helper.
- [x] The helper can be unit-tested with fixed timestamps.
</success_criteria>
